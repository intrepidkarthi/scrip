// Scrip Protocol — reference implementation.
//
// Deliberately zero-dependency. A protocol that drags a dependency tree into every
// adopter's build is one fewer reason to adopt it, and every dependency here would be
// a decision imposed on implementers about how they represent money, identity or time.
module github.com/intrepidkarthi/scrip

go 1.22
